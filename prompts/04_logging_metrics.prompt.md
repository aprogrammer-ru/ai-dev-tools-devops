Реализуй: "Логирование и метрики".

Ограничения:
- Не изменяй деплой в Kubernetes и GitHub Actions.
- Не внедряй компоненты ELK/Loki в этом шаге.

Сделай:
1. Включи структурированное JSON-логирование.
2. Добавь `request_id`/`correlation_id` в логи и в ответ API.
3. Экспортируй Prometheus-метрики:
   - RPS
   - latency
   - доля ошибок (4xx/5xx)
   - базовые метрики Redis/Postgres операций.
4. Обнови документацию по метрикам (какие метрики есть и зачем).

Критерий готовности:
- Метрики доступны по endpoint'у и логи содержат контекст запроса.
